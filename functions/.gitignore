# Deploy artifact (generated by build.mjs into ../functions-deploy/)
# Kept here for reference; the actual directory lives at repo root.

# Legacy compiled JS files (no longer produced — esbuild outputs to functions-deploy/)
lib/

# TypeScript v1 declaration files
typings/

# Node.js dependency directory
node_modules/
*.local

# Logs
*.log
firebase-debug.log*
